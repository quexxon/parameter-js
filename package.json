{
  "name": "@qxn/parameter",
  "version": "0.2.2",
  "description": "Dynamic scoping for JavaScript inspired by Racket's parameter construct",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/quexxon/parameter-js.git"
  },
  "scripts": {
    "prepack": "npm run build",
    "format": "prettier --write .",
    "prebuild": "rm -rf dist && npm run format",
    "build": "tsc",
    "pretest": "npm run build",
    "test": "tap"
  },
  "keywords": [],
  "author": "Will Clardy <will@quexxon.net>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.7.1",
    "tap": "^16.3.0",
    "typescript": "^4.8.4"
  }
}
